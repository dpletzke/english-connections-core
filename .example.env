# Copy this file to `.env` and update values before running the content
# manager CLI. The AWS SDK respects these environment variables when
# establishing credentials for the S3 upload flow.

# Use a named profile from ~/.aws/credentials (preferred).
AWS_PROFILE=

# Alternatively, supply static access keys. Leave commented when using profiles.
# AWS_ACCESS_KEY_ID=AKIAxxxxxxxxxxxxx
# AWS_SECRET_ACCESS_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Ensure the CLI and any supporting tooling target the São Paulo region.
AWS_REGION=sa-east-1

# Optional defaults for the econn-content CLI when --env/--dry-run flags are omitted.
# ECON_CONTENT_DEFAULT_ENV=dev
# ECON_CONTENT_DEFAULT_DRY_RUN=false
